# LLM Provider (optional — set one to enable AI-powered features)
# Without these, all features fall back to deterministic analysis

# Groq (recommended — fast, free tier available)
GROQ_API_KEY=

# OpenAI (alternative provider)
OPENAI_API_KEY=

# AWS Strands (optional — for agentic orchestration)
# ANTHROPIC_API_KEY=
